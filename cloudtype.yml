version: '1.0'

components:
  - name: web
    type: web
    env:
      - name: FLASK_ENV
        value: "production"
    build:
      dockerfile: Dockerfile
    mounts:
      - name: mydata
        path: /app
